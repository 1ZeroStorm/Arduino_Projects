#include <iostream>
#include <cmath>
using namespace std;

int main(){
    double a;
    double b = 4;
    double c = 3;

    a = sqrt(c*c + b*b);
    cout << a;
    
    
    // a = sqrt(c*c + b*b);
    // cout << a;
    return 0;
}